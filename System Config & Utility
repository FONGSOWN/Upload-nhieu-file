Code/shared/config.py

HOST = "127.0.0.1"
PORT = 5000
UPLOAD_FOLDER = "uploads"

Code/shared/file_utils.py

import os
from shared.config import UPLOAD_FOLDER


def create_upload_folder():
    if not os.path.exists(UPLOAD_FOLDER):
        os.makedirs(UPLOAD_FOLDER)

Cấu trúc:

Code/
├── shared/
│   ├── config.py
│   └── file_utils.py
└── uploads/
